import "./Btn.css";

const Btn = () => {
    return (
        <input
            type="submit"
            value="Przelicz"
            id="formBtn"
            className="Form-btn"
        />
    );
};

export default Btn;
